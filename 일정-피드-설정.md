# 일정 피드 설정 (게시된 구글 시트)

"일정에서 LED 추가" 기능은 **읽기 전용**이다 — 게시된 구글 시트 CSV에서 밴드
일정 목록을 읽어올 뿐, 일정 등록은 하지 않는다. 시트에 행을 채우는 것은
자동화(아래 3장)가 담당한다.

앱이 필요로 하는 값은 **`SCHEDULE_SHEET_CSV_URL` 하나**뿐이다
(`js/save/scheduleFeed.js` 상단, 현재 값 채워짐).

## 0. 데이터 흐름

```
밴드 일정 ──(자동화: Gmail→시트 Apps Script 등)──▶ 구글 시트
                                                      │ (웹에 게시: CSV)
노드앱 "밴드 일정" 목록 ◀──(fetch, CORS 허용)──────── 게시된 CSV
```

- 앱은 게시된 CSV의 행만 읽는다. **소스 무관** — Apps Script든 사람이 직접
  시트에 입력하든 상관없다. 첫 4열 순서(`타임스탬프 | 날짜 | 제목 | 일정 내용`)만
  지키면 된다(`scheduleFeed.js`의 `mapScheduleRows`가 인덱스 0~3으로 읽음).
- 본문(또는 제목)에 `3mm`, `7*3`(미터) 같은 표기가 있어야 파싱된다. 좌우/중앙
  멀티 구성은 `중앙 6*3, 좌우 3*2.5` 처럼 쓰면 LED 노드가 섹션 수만큼 생긴다.

## 1. 시트 준비 (이미 완료됨 — 재생성 시 참고)

1. 구글 시트 새로 만들기. 1행(헤더)에 `타임스탬프`, `날짜`, `제목`, `일정 내용`.
   - (선택) 구글 폼을 만들어 "응답을 스프레드시트로" 연결하면 이 헤더가 자동
     생성되고, 사람이 폼으로도 행을 추가할 수 있다.
2. **파일 → 공유 → 웹에 게시** → "링크" 탭:
   - 게시할 항목: 해당 시트 탭
   - 형식: **쉼표로 구분된 값(.csv)**
3. **게시** → URL 복사 (`.../pub?gid=...&single=true&output=csv`)
   = `SCHEDULE_SHEET_CSV_URL`.

> 게시 CSV는 실시간 시트보다 몇 분 지연될 수 있다. 방금 추가한 일정이 목록에
> 안 보이면 잠시 뒤 모달의 "새로고침".

## 2. 앱에 반영

`js/save/scheduleFeed.js`:

```js
const SCHEDULE_SHEET_CSV_URL = 'https://docs.google.com/spreadsheets/d/e/…/pub?gid=…&single=true&output=csv';
```

바꾼 뒤 CLAUDE.md "버전 업 규칙"대로 `APP_VERSION`·`package.json` version·
`service-worker.js` `CACHE_VERSION` 을 올리고 커밋.

## 3. 자동화: Outlook 캘린더 → 시트 (Apps Script)

`apps-script/outlook-sync.gs` 가 계산기앱도 쓰는 Outlook 공유 캘린더(ICS)를
15~30분마다 읽어 새 일정을 이 시트에 추가한다. 사람이 하는 일은 **밴드 일정 →
Outlook 캘린더** 뿐(계산기용으로 이미 하던 작업).

```
사람: 밴드 일정 → Outlook 공유 캘린더
Apps Script (kdj3531, 시트 바인딩, 30분 트리거):
   ICS fetch → VEVENT 파싱 → UID로 중복 제거 → 꼬리말 제거 → 시트에 행 추가
시트 → 게시 CSV → 노드앱
```

### 설치

1. 이 시트를 연다 → **확장 프로그램 → Apps Script**.
2. `apps-script/outlook-sync.gs` 내용을 통째로 붙여넣고 저장.
   - 맨 위 `TARGET_GID` 는 노드앱이 읽는(게시된) 탭의 gid — 지금 값 `608217936`.
     게시 CSV URL의 `gid=` 값과 같아야 한다.
3. 함수 목록에서 **`syncOutlookToSheet`** 선택 → **실행** → 권한 승인(1회).
   외부 URL 읽기 + 스프레드시트 쓰기 권한을 요구한다.
   - 처음 실행하면 최근 60일 이후 일정이 한꺼번에 들어온다(`DATE_CUTOFF_DAYS`).
4. 함수 목록에서 **`installTrigger`** 실행 → 30분 주기 트리거 설치.
   (또는 왼쪽 ⏰ 트리거 → 트리거 추가 → `syncOutlookToSheet` / 시간 기반 / 30분마다)

### 동작 메모

- 중복 제거: 처리한 VEVENT UID를 숨김 탭 `_seen` 에 적어둔다. 이 탭을 지우면
  기존 일정이 한 번 다시 들어올 수 있다.
- 설치면적(`N*M`) 표기가 없는 일정(예: "영상오퍼")은 시트에 넣지 않는다.
- Band→Outlook 변환 시 붙는 `" - A-TEAM(CJ) …"` 꼬리말은 잘라내고 넣는다.
- `3mm 18*5 중앙무대 … 4mm 20*5 좌우중계` 처럼 한 일정에 화면이 여러 개면
  노드앱이 **첫 번째(주 화면)만** LED 노드로 만든다 — 나머지는 수동 추가.
