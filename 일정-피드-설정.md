# 일정 피드 설정 (구글 폼 + 게시된 시트)

"일정에서 LED 추가" 기능이 쓰는 구글 폼·시트를 만드는 절차. 다 만든 뒤
`js/save/scheduleFeed.js` 상단의 **상수 5개**를 채우면 기능이 켜진다.
그 전까지는 모달 목록에 "일정 피드가 아직 설정되지 않았습니다"만 뜬다.

## 0. 개요 — 어떻게 동작하나

```
앱 모달 textarea ──(숨은 iframe <form> POST)──▶ 구글 폼 응답
                                                     │
                                              응답 스프레드시트
                                                     │ (웹에 게시: CSV)
앱 모달 "등록된 일정" 목록 ◀──(fetch, CORS 허용)──── 게시된 CSV
```

- 쓰기는 구글 폼 응답 제출이라 **로그인·CORS·서버가 필요 없다** (자매 기능
  `cloudShare.js`의 커뮤니티 프리셋 공유와 같은 방식).
- 읽기는 "웹에 게시"된 시트 CSV를 그냥 `fetch` (구글이 공개 게시 문서엔 CORS 허용).
- **소스 무관**: 앱은 CSV의 행만 읽는다. 지금은 사람이 밴드 메시지를 붙여넣어
  행이 쌓이지만, 나중에 Gmail→시트 Apps Script나 밴드 스크래퍼가 같은 열
  순서로 행을 추가해도 앱은 그대로 동작한다.

## 1. 구글 폼 만들기

1. <https://forms.google.com> → 빈 양식. 제목 예: "LED 일정 입력".
2. 질문 **3개**를 이 순서로 추가:
   | 질문 제목 | 유형 |
   |---|---|
   | `날짜` | 단답형 |
   | `제목` | 단답형 |
   | `일정 내용` | 장문형 |
3. 우상단 톱니(설정) → **응답** → "이메일 주소 수집" 끄기, "로그인 필요" 끄기.
   (누구나 로그인 없이 제출할 수 있어야 함.)

## 2. `entry.*` ID 와 제출 URL 뽑기

가장 쉬운 방법 — **미리 채워진 링크**:

1. 폼 편집 화면 우상단 ⋮ → **미리 채워진 링크 가져오기**.
2. 세 칸에 아무 임시값(예: `d`, `t`, `b`) 입력 → **링크 가져오기** → **링크 복사**.
3. 복사된 URL을 보면 이런 쿼리스트링이 있다:
   `...?usp=pp_url&entry.1111111=d&entry.2222222=t&entry.3333333=b`
   - `entry.1111111` = **날짜** → `SCHEDULE_FORM_ENTRY_DATE`
   - `entry.2222222` = **제목** → `SCHEDULE_FORM_ENTRY_TITLE`
   - `entry.3333333` = **일정 내용** → `SCHEDULE_FORM_ENTRY_BODY`
   (질문을 추가한 순서대로 나온다.)
4. **제출 URL**: 그 URL에서 `.../viewform...` 부분의 `viewform` 을 `formResponse`
   로 바꾼 것 = `SCHEDULE_FORM_RESPONSE_URL`.
   예: `https://docs.google.com/forms/d/e/1FAIpQL.../formResponse`

(대안: 실제 폼을 열어 한 번 제출하면서 브라우저 개발자도구 → Network 탭에서
`formResponse` POST 요청의 Form Data에 있는 `entry.*` 이름을 확인해도 된다.)

## 3. 응답 스프레드시트 연결

폼 편집 화면 → **응답** 탭 → 초록색 스프레드시트 아이콘 "**Sheets에 연결**"
→ 새 스프레드시트 생성.

시트의 첫 행(헤더)은 자동으로 `타임스탬프 | 날짜 | 제목 | 일정 내용` 이 된다.

## 4. 시트를 CSV로 "웹에 게시"

1. 그 스프레드시트에서 **파일 → 공유 → 웹에 게시**.
2. "링크" 탭에서:
   - 게시할 항목: **응답 시트 탭** 선택 (전체 문서 아님)
   - 형식: **쉼표로 구분된 값(.csv)**
3. **게시** → 나오는 URL 복사. `.../pub?gid=...&single=true&output=csv` 형태 =
   `SCHEDULE_SHEET_CSV_URL`.

> 게시된 CSV는 실시간 시트보다 몇 분 지연될 수 있다 — 방금 등록한 일정이
> 목록에 바로 안 보이면 잠시 뒤 "새로고침".

## 5. 상수 채우기

`js/save/scheduleFeed.js` 상단:

```js
const SCHEDULE_FORM_RESPONSE_URL = 'https://docs.google.com/forms/d/e/…/formResponse';
const SCHEDULE_FORM_ENTRY_DATE   = 'entry.1111111';
const SCHEDULE_FORM_ENTRY_TITLE  = 'entry.2222222';
const SCHEDULE_FORM_ENTRY_BODY   = 'entry.3333333';
const SCHEDULE_SHEET_CSV_URL     = 'https://docs.google.com/spreadsheets/d/e/…/pub?gid=0&single=true&output=csv';
```

채운 뒤 CLAUDE.md "버전 업 규칙"대로 `APP_VERSION`·`package.json` version·
`service-worker.js` `CACHE_VERSION` 을 올리고 커밋.

## 6. 주의

- **열 순서 고정**: 시트 첫 4열은 `타임스탬프 | 날짜 | 제목 | 일정 내용` 이어야
  한다(`scheduleFeed.js`의 `mapScheduleRows`가 인덱스 0~3으로 읽음). 폼에 질문을
  더 넣으려면 이 4개를 앞에 두거나 `mapScheduleRows`의 인덱스를 고칠 것.
- **줄바꿈·쉼표**: 장문형 본문의 줄바꿈·쉼표는 구글 게시 CSV가 큰따옴표로
  감싸므로 `parseCsv`(RFC4180)가 그대로 처리한다. 외부 자동화로 행을 추가할
  때도 이 규칙을 지킬 것.
- **피치/면적 표기**: 본문(또는 제목)에 `3mm`, `7*3`(미터) 같은 표기가 있어야
  파싱된다. 좌우/중앙 멀티 구성은 `중앙 6*3, 좌우 3*2.5` 처럼 쓰면 LED 노드가
  섹션 수만큼 생긴다.
