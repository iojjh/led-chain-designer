# 일정 피드 설정 (Outlook ICS + Apps Script 프록시)

"일정에서 LED 추가" 기능은 **읽기 전용**이다 — 계산기앱도 쓰는 Outlook 공유
캘린더의 ICS를 읽어 목록을 만들 뿐이다. 사람이 하는 일은 **밴드 일정 → Outlook
캘린더**뿐(계산기용으로 이미 하던 작업).

## 0. 왜 Apps Script 프록시가 필요한가

Outlook ICS 응답에는 CORS 헤더가 없어 브라우저(노드앱)에서 직접 `fetch` 할 수
없다. 무료 공개 프록시(corsproxy.io 등)는 현재 게이트/불안정하다. 그래서 구글
Apps Script를 "웹 앱"으로 배포해 **직접 소유한 프록시**를 만든다 — doGet이
서버에서 ICS를 받아 CORS가 열린 응답으로 돌려준다. 시트도, 트리거도, 지연도
없다.

```
사람: 밴드 일정 → Outlook 공유 캘린더
노드앱: ics-proxy.gs(/exec) fetch → ICS 파싱(scheduleFeed.js) → 목록 → "가져오기" → LED 노드
```

## 1. Apps Script 프록시 배포

1. <https://script.google.com> → **새 프로젝트**.
2. `apps-script/ics-proxy.gs` 내용을 통째로 붙여넣고 저장.
   - 캘린더를 바꾸려면 맨 위 `ICS_URL` 만 고친다.
3. **배포 → 새 배포** → 유형 **웹 앱**:
   - 설명: 아무거나 (예: `ics proxy v1`)
   - 실행: **나(kdj3531@gmail.com)**
   - 액세스 권한: **모든 사용자**
4. **배포** → 처음이면 권한 승인(외부 URL 읽기).
5. 나오는 **웹 앱 URL**(`https://script.google.com/macros/s/…/exec`)을 복사.

## 2. 앱에 반영

`js/save/scheduleFeed.js` 상단:

```js
const SCHEDULE_ICS_PROXY_URL = 'https://script.google.com/macros/s/…/exec';
```

바꾼 뒤 CLAUDE.md "버전 업 규칙"대로 `APP_VERSION`·`package.json` version·
`service-worker.js` `CACHE_VERSION` 을 올리고 커밋.

빈 문자열이면 기능 비활성 — 모달 목록에 "설정되지 않았습니다"만 뜨고 요청을
하지 않는다.

## 3. 동작 메모

- 목록은 **시작일이 오늘 -7일 이후**인 일정만, 날짜 오름차순(가까운 것부터).
  기준은 `scheduleFeed.js`의 `SCHEDULE_RECENT_DAYS`.
- 설치면적(`N*M`) 표기가 없는 일정(예: "영상오퍼")은 목록에서 뺀다.
- Band→Outlook 변환 시 붙는 `" - A-TEAM(CJ) …"` 꼬리말은 잘라내고 파싱한다.
- `3mm 18*5 중앙무대 … 4mm 20*5 좌우중계` 처럼 한 일정에 화면이 여러 개면
  **첫 번째(주 화면)만** LED 노드로 만든다 — 나머지는 수동 추가.
- 매번 새로 fetch하므로 중복/상태 관리가 없다. 캘린더를 바꾸면 `ics-proxy.gs`의
  `ICS_URL` 만 고쳐 재배포(배포 관리 → 편집 → 새 버전).
